# DevTinder

- Create Vite + React Application.
- Remove Unnecessary code create hello world app
- Install Tailwind CSS
- Install Daisy UI Component library
- Add nav bar component to App.jsx
- Install react-router package
- Create BrowserRouter > Routes > Route > Route Children
- Create an Outlet in Body Component to import Children
- Create Footer Component
- Create Login Component
- Install axios
- CORS - Install cors in Backend => Add middleware to with configuration: origin, credentials: true
- Whenever you're making an API call so pass axios => { withCredentials : true }
- Install react-redux + @reduxjs/toolkit => configurestore => add providers in for Route => create slice => add reducer to store.
- Add redux devtools in chrome
- Login and see if your data is coming properly in the store.
- Navbar should update as soon as user logs in
- Refactor our code add constant file and component folder.
- You should not access other routes without login
- If token is not present redirect to login page
- Logout feature
- Get the feed and add feed in the store.
- Build the user card on feed
- Edit profile feature
- Show toast message on the save of profile
- New Page - See all my connections
- New Page - See all my requests
- Feature accept or reject connection requests
